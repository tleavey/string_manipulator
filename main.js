const StringManipulation = require('./StringManipulation.js');

console.log(StringManipulation.repeatedWords('Example of using the StringManipulation class because an example can be helpful.'));

console.log(StringManipulation.removePunctuation('Example of using the StringManipulation class because an example can be helpful.'));